import Vue from 'vue';
import { VTooltip } from 'v-tooltip';
// Here we should import directives that we can directly implement into every vue components
//import './lowercase'

//temporary @todo
Vue.directive('my-tooltip', VTooltip);